import React from "react";
import { ScrollView, Text } from "react-native";
import CategoryFilterin from "../../components/CategoryFiltering";

function index() {
  return (
    <ScrollView>
      <CategoryFilterin />
    </ScrollView>
  );
}

export default index;
